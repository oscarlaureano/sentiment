# sentiment

### Instalación

Tecnología:
- python3

Librerias requeridas:
- Flask

`pip install flask`

- Dash

`pip install dash==1.4.0 `

`pip install dash-daq==0.2.1 `


### Para correr

` python dashApp.py `